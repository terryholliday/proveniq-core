import { SIMULATED_METRICS } from "@/lib/mdx";

# Proveniq Home

The entry point for institutional asset ingestion.

## Architecture

Home serves as the primary gateway for converting Real World Assets (RWA) into digital instrument clusters.

## Performance Metrics

<div className="my-6 p-4 border border-slate-800 bg-slate-900 rounded-lg">
  <div className="text-xs font-mono text-slate-500 mb-2">{SIMULATED_METRICS.label}</div>
  <div className="grid grid-cols-3 gap-4">
    <div>
        <div className="text-slate-400 text-sm">Throughput</div>
        <div className="text-2xl font-bold text-sky-500">{SIMULATED_METRICS.tps} TPS</div>
    </div>
     <div>
        <div className="text-slate-400 text-sm">Latency</div>
        <div className="text-2xl font-bold text-emerald-500">{SIMULATED_METRICS.latency}</div>
    </div>
     <div>
        <div className="text-slate-400 text-sm">Uptime</div>
        <div className="text-2xl font-bold text-white">{SIMULATED_METRICS.uptime}</div>
    </div>
  </div>
</div>

## Integration

Connects directly to the **Core** orchestration layer.
